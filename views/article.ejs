<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Щуропедия</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1><a href="/">Щуропедия</a></h1>
        <div class="auth-links">
            <% if (user) { %>
                <span>Вы вошли как: <strong><%= user %></strong></span>
                <form method="POST" action="/logout" style="display: inline;">
                    <button type="submit">Выйти</button>
                </form>
                <a href="/create">Создать статью</a>
            <% } else { %>
    <a href="/admin">🔐 Вход админа</a>
<% } %>
        </div>
    </header>
    <main>
        <div class="content">
            <h1><%= title %></h1>
            <div>
                <%- content %>
            </div>
            
            <% if (user) { %>
                <div style="margin-top: 20px;">
                    <a href="/edit/<%= title %>">Редактировать</a> | 
                    <a href="/history/<%= title %>">История</a> | 
                    <a href="/create">Создать новую</a>
                </div>
            <% } %>
        </div>
		<div class="article-actions">
    <% if (user) { %>
        <a href="/edit/<%= title %>" class="action-btn primary">✏️ Редактировать</a>
        <a href="/history/<%= title %>" class="action-btn">📜 История</a>
        <button class="action-btn favorite-btn" onclick="toggleFavorite(<%= article.id %>)">⭐ В избранное</button>
    <% } %>
</div>

<!-- ДОБАВЬ блок комментариев: -->
<div class="comments-section">
    <h3>💬 Комментарии</h3>
    
    <% if (user) { %>
        <form method="POST" action="/comment/<%= article.id %>" class="comment-form">
            <input type="hidden" name="articleTitle" value="<%= title %>">
            <textarea name="content" placeholder="Ваш комментарий..." required></textarea>
            <button type="submit" class="action-btn primary">Отправить</button>
        </form>
    <% } %>
    
    <!-- Здесь будут комментарии -->
</div>
    </main>
</body>
</html>