<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Щуропедия</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1><a href="/">Оплот мудрости шуриизма</a></h1>
        <input type="text" id="search" placeholder="Поиск по вики...">
    </header>
	<div class="auth-links">
    <% if (user) { %>
        <span class="user-info">
            Вы вошли как: <strong><%= user %></strong>
            <form method="POST" action="/logout" style="display: inline;">
                <button type="submit">Выйти</button>
            </form>
            <a href="/create">Создать статью</a>
        </span>
    <% } else { %>
        <a href="/login">Войти</a> | <a href="/register">Регистрация</a>
    <% } %>
</div>
    <main>
        <div class="sidebar">
            <h3>Все статьи</h3>
            <ul>
                <% articles.forEach(function(article) { %>
                    <li><a href="/article/<%= article %>"><%= article %></a></li>
                <% }); %>
            </ul>
        </div>
        <div class="content">
            <h2>Добро пожаловать в Щуропедию</h2>
            <p>Это база знаний щуриизма. Здесь вы можете хранить статьи, заметки и другую важную информацию для щуриисткого учения.</p>
            
            <h3>Как пользоваться:</h3>
            <ul>
                <li>Выберите статью из списка слева</li>
                <li>Используйте поиск для быстрого перехода</li>
                <li>Создавайте новые статьи в папке <code>articles/</code></li>
            </ul>
        </div>
    </main>
    <script src="/script.js"></script>
	<div class="theme-switcher">
    <button class="theme-toggle" onclick="toggleTheme()">🌙</button>
</div>

<script>
function toggleTheme() {
    document.body.classList.toggle('dark-theme');
    const isDark = document.body.classList.contains('dark-theme');
    localStorage.setItem('darkTheme', isDark);
    document.querySelector('.theme-toggle').textContent = isDark ? '☀️' : '🌙';
}

// Загрузка темы из localStorage
if (localStorage.getItem('darkTheme') === 'true') {
    document.body.classList.add('dark-theme');
    document.querySelector('.theme-toggle').textContent = '☀️';
}
</script>
<div class="theme-switcher">
    <button class="theme-toggle" onclick="toggleTheme()">🌙</button>
</div>

<script>
function toggleTheme() {
    document.body.classList.toggle('dark-theme');
    const isDark = document.body.classList.contains('dark-theme');
    localStorage.setItem('darkTheme', isDark);
    document.querySelector('.theme-toggle').textContent = isDark ? '☀️' : '🌙';
}

// Загрузка темы из localStorage
if (localStorage.getItem('darkTheme') === 'true') {
    document.body.classList.add('dark-theme');
    document.querySelector('.theme-toggle').textContent = '☀️';
}
</script>
</body>
</html>