<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Щуропедия</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1><a href="/">Оплот мудрости шуриизма</a></h1>
        <form method="GET" action="/search" class="search-form">
            <input type="text" name="q" placeholder="Поиск по статьям...">
            <button type="submit">🔍</button>
        </form>
    </header>
    <div class="auth-links">
        <% if (user) { %>
            <span>Админ: <strong><%= user %></strong></span>
            <form method="POST" action="/logout" style="display: inline;">
                <button type="submit">Выйти</button>
            </form>
            <a href="/create">Создать статью</a>
        <% } else { %>
            <a href="/admin">Вход для редактирования</a>
        <% } %>
    </div>
    <main>
        <div class="sidebar">
            <h3>Все статьи</h3>
            <ul>
                <% articles.forEach(function(article) { %>
                    <li><a href="/article/<%= article %>"><%= article %></a></li>
                <% }); %>
            </ul>
        </div>
        <div class="content">
            <h2>Добро пожаловать в Щуропедию</h2>
            <p>Это база знаний щуриизма. Здесь вы можете хранить статьи, заметки и другую важную информацию для щуриисткого учения.</p>
            
            <h3>Как пользоваться:</h3>
            <ul>
                <li>Выберите статью из списка слева</li>
                <li>Используйте поиск для быстрого перехода</li>
                <li>Создавайте новые статьи через меню</li>
            </ul>
            
            <div style="margin-top: 2rem; padding: 1.5rem; background: #f8f9fa; border-radius: 8px;">
                <h4>Быстрые ссылки:</h4>
                <p>
                    <a href="/create" style="color: #36c; text-decoration: none; font-weight: 500;">Создать статью</a> | 
                    <a href="/categories" style="color: #36c; text-decoration: none; font-weight: 500;">Категории</a> | 
                    <a href="/search" style="color: #36c; text-decoration: none; font-weight: 500;">Поиск</a>
                </p>
            </div>
        </div>
    </main>
    <div class="theme-switcher">
        <button class="theme-toggle" onclick="toggleTheme()">🌙</button>
    </div>
    <script>
    function toggleTheme() {
        document.body.classList.toggle('dark-theme');
        const isDark = document.body.classList.contains('dark-theme');
        localStorage.setItem('darkTheme', isDark);
        document.querySelector('.theme-toggle').textContent = isDark ? '☀️' : '🌙';
    }
    if (localStorage.getItem('darkTheme') === 'true') {
        document.body.classList.add('dark-theme');
        document.querySelector('.theme-toggle').textContent = '☀️';
    }
    </script>
</body>
</html>