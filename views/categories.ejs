<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Категории - Щуропедия</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1><a href="/">Щуропедия</a></h1>
        <div class="auth-links">
            <% if (user) { %>
                <span>Вы вошли как: <strong><%= user %></strong></span>
                <form method="POST" action="/logout" style="display: inline;">
                    <button type="submit">Выйти</button>
                </form>
                <a href="/create">Создать статью</a>
           <% } else { %>
    <a href="/admin">🔐 Вход админа</a>
<% } %>
        </div>
    </header>

    <main>
        <div class="categories-page">
            <h2>📚 Категории статей</h2>
            
            <div class="categories-grid">
                <% categories.forEach(category => { %>
                    <a href="/category/<%= category.name %>" class="category-card">
                        <h3><%= category.name %></h3>
                        <p class="description"><%= category.description || 'Описание категории' %></p>
                        <p class="count"><%= category.articles_count %> статей</p>
                    </a>
                <% }); %>
            </div>
        </div>
    </main>

    <div class="theme-switcher">
        <button class="theme-toggle" onclick="toggleTheme()">🌙</button>
    </div>

    <script>
    function toggleTheme() {
        document.body.classList.toggle('dark-theme');
        const isDark = document.body.classList.contains('dark-theme');
        localStorage.setItem('darkTheme', isDark);
        document.querySelector('.theme-toggle').textContent = isDark ? '☀️' : '🌙';
    }

    if (localStorage.getItem('darkTheme') === 'true') {
        document.body.classList.add('dark-theme');
        document.querySelector('.theme-toggle').textContent = '☀️';
    }
    </script>
</body>
</html>